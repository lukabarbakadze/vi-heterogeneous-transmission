# Python dependencies (versions used for the thesis runs)
pandas==2.3.3
numpy==2.4.0
pyarrow==22.0.0
openpyxl>=3.1        # read the SF Fed .xlsx
matplotlib>=3.8      # figures
tqdm>=4.66           # progress bars in the Compustat build
